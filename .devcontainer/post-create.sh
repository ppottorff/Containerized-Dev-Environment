#! /bin/bash
# stuff to do on container before using it
sudo apt update -y
sudo apt upgrade -y
// sudo apt install awscli -y - ADDED to features
// sudo apt install nodejs -y - ADDED to features
// npm install -g npm@9.1.1 SEE if needed
// npm install -g aws-cdk -y ADDED to features
